<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message Details</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        color: #333;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
      }

      h1 {
        margin-top: 20px;
        color: #444;
      }

      .message-container {
        background-color: #fff;
        padding: 20px 30px;
        margin-top: 20px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        width: 80%;
        max-width: 500px;
      }

      h3 {
        margin-bottom: 10px;
        color: #007acc;
      }

      p {
        margin: 5px 0;
        font-size: 1rem;
      }
      a{
        text-decoration: none;
        color: #007acc;
        margin-top: 1rem;
      }
    </style>
  </head>
  <body>
    <h1>Message Details</h1>
    <div class="message-container">
      <h3><%= msg.msg %></h3>
      <p><strong>User:</strong> <%= msg.user %></p>
      <p><strong>Date:</strong> <%= msg.dateofpost.toString().slice(0,10) %></p>
    </div>
    <a href="/">Back to Home</a>
  </body>
</html>
